# Logs

The **Logs** tab shows the application activity log:

- UI messages (import/run/errors)
- browser session start/stop events

Logs are also written to `logs/` (including `logs/proxy.log` for proxy diagnostics).
